# Permite compilar los klibs de iosArm64 desde Linux (verificación en local).
# El ENLACE del framework sigue exigiendo macOS: lo hace CI en katapult-go.yml.
kotlin.native.enableKlibsCrossCompilation=true
